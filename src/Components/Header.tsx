import { defaultHead } from "next/head";

const Header = () => {
    return (
        <>
        <header>
            <p>Aqui vai o header</p>
        </header>
        </>
    );
};
export default Header;